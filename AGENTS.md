# 章节开发与 GitHub 提交约定

用户要求：每完成一章代码，都单独提交并推送 GitHub，以便查看该章的变化。

- 在当前项目持续开发，保持前几章功能可用，按用户提供的章节范围实现。
- 每章完成后执行适当测试，并验证实际启动；要求 IDEA 验证时，必须检查 IDEA 的运行状态。
- 更新 `docs/chapters/` 对应章节说明、`CHANGELOG.md` 和 README 的章节导航，说明改动、验证和限制。
- 每章建立独立提交，标题使用 `第N章：具体能力`；正文说明主要改动和验证结果。
- 每章的可运行版本增加 `chapter-NN` 标签，例如 `chapter-04`。已存在的标签不要移动；修正使用新提交，并按需要增加修订标签。
- 将本章提交及新增标签推送到已有 `origin`，完成后提供 GitHub 提交或差异链接。不得用强制推送覆盖远程历史。
- 提交前检查差异，只包含本次授权范围的文件，不混入用户尚未确认归属的其他改动。
- 不提交 `.idea/`、`target/`、凭证或本地日志。API Key 只从 `DASHSCOPE_API_KEY` 环境变量读取。
- 前三章的 Git 历史在 2026-09-12 根据本任务中的代码与文档补建；不要将其表述为当时已实时提交。
